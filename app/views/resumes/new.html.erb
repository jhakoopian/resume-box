<h1>Upload Resume</h1>
<div class="row">
  <%= form_for @resume do |f| %>
    <div class="form-group col-md-7">
      <%= f.text_field :name, class: 'form-control', placeholder: "Enter a name for your resume" %>
    </div>
    <div class="form-group col-md-1">
      <%= f.submit "Save", class: 'btn btn-primary' %>
    </div>
  <% end %>
</div>
